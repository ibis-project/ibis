# Need to import double-conversion below otherwise `import pyarrow` fails with ImportError: libdouble-conversion.so.3
# We can remove the import once pyarrow is a higher version
double-conversion
pyarrow>=0.12, <=0.14.1
pyspark>=2.4.3, <3.0.0
