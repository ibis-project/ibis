SELECT
  (
    {'i': (
      {'i': (
        {'i': (
          {'i': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."i" + 1, 's': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."s" || 'bar'}
        )."i" + 1, 's': (
          {'i': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."i" + 1, 's': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."s" || 'bar'}
        )."s" || 'bar'}
      )."i" + 1, 's': (
        {'i': (
          {'i': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."i" + 1, 's': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."s" || 'bar'}
        )."i" + 1, 's': (
          {'i': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."i" + 1, 's': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."s" || 'bar'}
        )."s" || 'bar'}
      )."s" || 'bar'}
    )."i" + 1, 's': (
      {'i': (
        {'i': (
          {'i': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."i" + 1, 's': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."s" || 'bar'}
        )."i" + 1, 's': (
          {'i': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."i" + 1, 's': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."s" || 'bar'}
        )."s" || 'bar'}
      )."i" + 1, 's': (
        {'i': (
          {'i': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."i" + 1, 's': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."s" || 'bar'}
        )."i" + 1, 's': (
          {'i': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."i" + 1, 's': (
            {'i': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."i" + 1, 's': (
              CAST(ROW(123, 'foo') AS STRUCT("i" TINYINT, "s" TEXT))
            )."s" || 'bar'}
          )."s" || 'bar'}
        )."s" || 'bar'}
      )."s" || 'bar'}
    )."s" || 'bar'}
  )."i" AS "i"