version: 2


jobs:
  test:
    machine:
      image: circleci/classic:latest
      docker_layer_caching: true

    steps:
      - checkout
      - run: ls -lah
      - run: bash testing/start-all.sh


workflows:
  version: 2
  test:
    jobs:
      - test

