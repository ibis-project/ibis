version: 2


jobs:

  test:
    docker:
      - image: ypereirareis/docker-compose
    steps:
      - checkout
      - setup_remote_docker
      - run: cd testing
      - run: bash start-all.sh


workflows:
  version: 2
  test:
    jobs:
      - test

