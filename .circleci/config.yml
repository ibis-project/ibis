version: 2


jobs:

  test:
    docker:
      - image: ypereirareis/docker-compose
    steps:
      - checkout
      - setup_remote_docker
      - run: |
          docker-compose up -d
      - run: docker ps
    

workflows:
  version: 2
  test:
    jobs:
      - test

