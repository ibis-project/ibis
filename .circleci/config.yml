version: 2


jobs:

  test:
    docker:
      - image: ypereirareis/docker-compose
    steps:
      - checkout
      - setup_remote_docker
      - run: |
          cd testing
          docker-compose up -d
          docker ps

workflows:
  version: 2
  test:
    jobs:
      - test

